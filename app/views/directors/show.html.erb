<h1><%= @director.name %></h1>

<h2>Filmes deste Diretor:</h2>

<% @movies.each do |movie| %>
    <li><%= link_to movie.title, movie_path(movie.id) %><br> </li>
  <% end %>
  <br />

<%= link_to 'Editar', edit_director_path %>
<br />
<%= link_to 'Voltar', directors_path %>
